name: feature
description: Generate Flutter clean architecture feature

version: 0.1.0+1

environment:
  mason: ^0.1.0

vars:
  name:
    type: string
    description: Feature name
    prompt: What is the feature name?